const UserDetails = ({ userDetails }) => {
  return (
    <div className="container ">
      <div className="row justify-content-center">{userDetails}</div>
    </div>
  );
};
export default UserDetails;
